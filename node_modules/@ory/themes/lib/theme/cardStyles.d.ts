import { ThemeProps } from './index';
export interface CardStyles extends ThemeProps {
    wide?: boolean;
}
export declare const cardStyles: ({ theme, wide }: CardStyles) => string;
export declare const cardTitleStyles: ({ theme }: CardStyles) => string;
