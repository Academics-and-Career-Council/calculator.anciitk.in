import { ThemeProps } from './helpers';
export interface AlertStyles extends ThemeProps {
    severity?: 'error' | 'info';
}
export declare const alertStyles: ({ theme, severity }: AlertStyles) => string;
export declare const alertContentStyles: () => string;
