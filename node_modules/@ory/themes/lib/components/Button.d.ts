import React, { ReactNode } from 'react';
import { ButtonStyles } from '../theme';
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>, ButtonStyles {
    helper?: ReactNode;
    children: string;
}
declare const _default: import("styled-components").StyledComponent<({ helper, className, ...props }: ButtonProps) => JSX.Element, any, {}, never>;
export default _default;
